import { ReactNode } from 'react';

//eslint-disable-next-line no-empty-pattern
export const WFTab = ({}: {
    label: string;
    value?: number | string;
    content: ReactNode;
}) => {
    return null;
};
